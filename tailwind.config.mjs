// Tailwind v4 is CSS-first — the real configuration lives in src/styles/global.css
// inside the @theme block. This file is kept as a stub for editor tooling that
// still expects a config file. Everything below is optional in v4.
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,ts,tsx,md,mdx,vue,svelte}'],
};
